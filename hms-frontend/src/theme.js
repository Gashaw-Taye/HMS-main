import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#0052cc',
      light:"skyblue"
    },
    secondary: {
      main: '#edf2ff',
    },
    other_color:{
        main:"#edf2ff",
        light:"skyblue"
    }
  },
});

